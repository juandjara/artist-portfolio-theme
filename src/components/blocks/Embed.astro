---
import { transformMediaInHTML } from "../../lib/directus"
import type { BlockEmbed } from "../../lib/directus.types"

interface Props {
  block: BlockEmbed
}

const block = Astro.props.block
const content = transformMediaInHTML(block.embed_code ?? undefined)
---

<Fragment set:html={content} />
